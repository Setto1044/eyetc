spring:
  application:
    name: signaling

server:
  port: 8080
  ssl:
    key-store: ${SSL_KEY_STORE}
    key-store-password: ${SSL_KEY_STORE_PASSWORD}
    key-store-type: PKCS12
    key-alias: eyetc

logging:
  level:
    root: INFO
    org.hibernate: ERROR